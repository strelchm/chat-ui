// @ts-ignore
import { createStore } from 'vuex'
import credentials from "@/store/credentials";

// @ts-ignore
export default createStore({
  // state: {
  // },
  // mutations: {
  // },
  // actions: {
  // },
  modules: {
      // @ts-ignore
      credentials
    }
})
